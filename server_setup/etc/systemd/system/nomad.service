[Unit]
Description=Nomad uWSGI instance
After=network.target

[Service]
PIDFile=/tmp/nomad.pid
ExecStart=/bin/sh -c "${HOME}/.virtualenv/Nomad/bin/uwsgi --ini /tmp/uwsgi.ini"

[Install]
WantedBy=multi-user.target